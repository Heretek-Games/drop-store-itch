{
  "name": "drop-store-itch",
  "version": "0.1.0",
  "description": "itch.io local library scanner client plugin for Drop (#21).",
  "type": "module",
  "main": "dist/src/index.js",
  "scripts": {
    "build": "rm -rf dist && tsc",
    "test": "node --test dist/test/*.js",
    "sign": "drop-plugin sign ."
  },
  "dependencies": {
    "@droposs/plugin-sdk": "file:../drop-plugin-sdk/packages/plugin-sdk"
  },
  "devDependencies": {
    "@droposs/plugin-cli": "file:../drop-plugin-sdk/packages/plugin-cli",
    "typescript": "^5.7.0",
    "@types/node": "^22.0.0"
  },
  "license": "MIT"
}